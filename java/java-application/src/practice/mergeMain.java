package practice;

public class mergeMain {
    public static void main(String[] args) {
        merge merge = new merge();
        merge.Choi("최문석");

        int result = merge.calcul(4, 5);
        System.out.println(result);
    }
}
